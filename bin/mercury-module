#!/usr/bin/python

import logging
import sys

from mwr.common import logger
from mwr.droidhg.repoman import ModuleManager

logger.setLevel(logging.DEBUG)
logger.addStreamHandler()

if __name__ == '__main__':
    ModuleManager().run(sys.argv[1::])
    